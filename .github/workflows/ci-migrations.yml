name: Run DB Migrations

on:
  push:
    branches: [develop]
    paths:
      - 'database/migrations/**'
      - '.github/workflows/ci-migrations.yml'
  workflow_dispatch:

jobs:
  migrate:
    runs-on: infatium
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install alembic psycopg2-binary

      - name: Stamp to sync alembic_version if needed
        working-directory: database/migrations
        env:
          DATABASE_URL: ${{ secrets.DEV_DATABASE_URL }}
        run: |
          CURRENT=$(alembic current 2>/dev/null | grep -oP '^[a-f0-9]+' || echo "none")
          echo "Current alembic version: $CURRENT"
          # Fix: marketplace_feeds + slug_to_tags were applied manually, stamp past them
          if [ "$CURRENT" = "c3d4e5f6a7b8" ]; then
            echo "Stamping to b3c4d5e6f7a9 (marketplace + slug migrations already applied manually)"
            alembic stamp b3c4d5e6f7a9
          fi

      - name: Run migrations
        working-directory: database/migrations
        env:
          DATABASE_URL: ${{ secrets.DEV_DATABASE_URL }}
        run: alembic upgrade head

      - name: Send Telegram notification
        if: always()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_DEPLOY_BOT_TOKEN }}
        run: |
          if [[ "${{ job.status }}" == "success" ]]; then
            STATUS="✅"
          else
            STATUS="❌"
          fi

          COMMIT_MSG=$(echo "${{ github.event.head_commit.message }}" | head -c 100 | sed 's/"/\\"/g')

          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -H "Content-Type: application/json" \
            -d "{
              \"chat_id\": \"-1002622491758\",
              \"message_thread_id\": 504,
              \"parse_mode\": \"HTML\",
              \"text\": \"${STATUS} <b>DB Migrations DEV</b>\n\n📝 Commit: ${COMMIT_MSG}\n👤 Author: ${{ github.event.head_commit.author.name }}\"
            }"
