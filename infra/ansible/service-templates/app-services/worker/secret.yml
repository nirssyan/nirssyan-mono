apiVersion: v1
kind: Secret
metadata:
  name: SERVICE_NAME-secrets
type: Opaque
stringData: {}
  # Add your secrets here via Lens UI, kubectl, or create-secrets.yml playbook
  # Example:
  # DATABASE_URL: "postgresql://user:password@host:5432/dbname"
  # API_KEY: "your-api-key-here"
  # JWT_SECRET: "your-jwt-secret"
