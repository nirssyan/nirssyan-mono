apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-api-python
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - dev.api.infatium.ru
      secretName: api-tls
  rules:
    - host: dev.api.infatium.ru
      http:
        paths:
          - path: /auth
            pathType: Prefix
            backend:
              service:
                name: app-auth-go
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app-api-go
                port:
                  number: 8080
