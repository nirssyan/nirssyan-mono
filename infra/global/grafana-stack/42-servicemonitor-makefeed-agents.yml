apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: makefeed-agents
  namespace: infrastructure
  labels:
    app: makefeed-agents
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: makefeed-agents
  namespaceSelector:
    matchNames:
      - infatium-dev
      - infatium-prod
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
