apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: makefeed-api
  namespace: infrastructure
  labels:
    app: makefeed-api
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: app-api-go
  namespaceSelector:
    matchNames:
      - infatium-dev
      - infatium-prod
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
