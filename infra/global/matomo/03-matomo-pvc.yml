apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matomo-data
  namespace: infrastructure
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
